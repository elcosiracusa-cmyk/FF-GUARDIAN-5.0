<Window x:Class="FFGuardian.PremiumWpf.MainWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="clr-namespace:FFGuardian.PremiumWpf"
 Title="FFGuardian Ultimate Protection"
 Width="1600" Height="960" MinWidth="1180" MinHeight="700"
 WindowStartupLocation="CenterScreen"
 Background="{StaticResource BackgroundBrush}"
 Foreground="{StaticResource TextBrush}"
 FontFamily="{StaticResource AppFont}"
 UseLayoutRounding="True"
 SnapsToDevicePixels="True">
 <Window.Resources>
  <Style x:Key="SidebarLabel" TargetType="TextBlock" BasedOn="{StaticResource SidebarTextStyle}">
   <Style.Triggers>
    <DataTrigger Binding="{Binding IsChecked, ElementName=SidebarToggle}" Value="True">
     <Setter Property="Visibility" Value="Collapsed"/>
    </DataTrigger>
   </Style.Triggers>
  </Style>
  <Style x:Key="SidebarItemIcon" TargetType="Path" BasedOn="{StaticResource SidebarIconStyle}">
   <Setter Property="Margin" Value="2,0,12,0"/>
  </Style>
 </Window.Resources>

 <Grid Background="{StaticResource BackgroundBrush}" TextElement.Foreground="{StaticResource TextBrush}">
  <Grid.ColumnDefinitions>
   <ColumnDefinition Width="Auto"/>
   <ColumnDefinition Width="*"/>
  </Grid.ColumnDefinitions>

  <Border Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
   <Border.Style>
    <Style TargetType="Border">
     <Setter Property="Width" Value="252"/>
     <Style.Triggers>
      <DataTrigger Binding="{Binding IsChecked, ElementName=SidebarToggle}" Value="True"><Setter Property="Width" Value="72"/></DataTrigger>
     </Style.Triggers>
    </Style>
   </Border.Style>
   <Grid>
    <Grid.RowDefinitions><RowDefinition Height="88"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

    <Grid Margin="14,12">
     <Grid.ColumnDefinitions><ColumnDefinition Width="44"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
     <Viewbox Width="38" Height="38">
      <Grid Width="24" Height="24">
       <Path Data="M12,2 L20,5 V11 C20,17 16,21 12,23 C8,21 4,17 4,11 V5 Z" Fill="#10253A" Stroke="{StaticResource AccentBrush}" StrokeThickness="1.5"/>
       <Path Data="M8,8 H16 M8,12 H14 M8,16 H12" Stroke="{StaticResource InfoBrush}" StrokeThickness="1.3"/>
      </Grid>
     </Viewbox>
     <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
      <StackPanel.Style><Style TargetType="StackPanel"><Style.Triggers><DataTrigger Binding="{Binding IsChecked, ElementName=SidebarToggle}" Value="True"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></StackPanel.Style>
      <TextBlock Text="FFGuardian" FontSize="19" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
      <TextBlock Text="Ultimate Protection" Style="{StaticResource CaptionTextStyle}" Foreground="{StaticResource MutedBrush}"/>
      <TextBlock Text="Versione 10.0.1" Style="{StaticResource CaptionTextStyle}" Foreground="{StaticResource MutedBrush}"/>
     </StackPanel>
    </Grid>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
     <StackPanel>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="dashboard" ToolTip="Dashboard"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconHome}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Dashboard" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="scan" ToolTip="Scansione"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconScan}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Scansione" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="realtime" ToolTip="Protezione tempo reale"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconShield}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Protezione tempo reale" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="ransom-shield" ToolTip="Ransom Shield"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconRansom}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Ransom Shield" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="firewall" ToolTip="Firewall"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconFirewall}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Firewall" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="usb-shield" ToolTip="USB Shield"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconUsb}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="USB Shield" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="quarantine" ToolTip="Quarantena"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconBox}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Quarantena" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="security-center" ToolTip="Security Center"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconCenter}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Security Center" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="updates" ToolTip="Aggiornamenti"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconUpdate}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Aggiornamenti" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="audit" ToolTip="Report"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconReport}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Report" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="pc-health" ToolTip="Salute PC"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconHealth}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Salute PC" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="tools" ToolTip="Strumenti"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconTools}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Strumenti" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="recovery" ToolTip="Recupero file"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconBox}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Recupero file" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="activity" ToolTip="Attività"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconCenter}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Attività" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="settings" ToolTip="Impostazioni"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconSettings}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Impostazioni" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
      <Button Style="{StaticResource NavButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="support" ToolTip="Assistenza"><StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconHelp}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Assistenza" Style="{StaticResource SidebarLabel}"/></StackPanel></Button>
     </StackPanel>
    </ScrollViewer>

    <StackPanel Grid.Row="2" Margin="10">
     <Border Style="{StaticResource StatusChipStyle}" Margin="0,0,0,8">
      <StackPanel><TextBlock Text="Stato protezione" Style="{StaticResource CaptionTextStyle}" Foreground="{StaticResource MutedBrush}"/><TextBlock Text="{Binding ProtectionText}" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" TextWrapping="Wrap"/><TextBlock Text="Ultima verifica: {Binding LastUpdate}" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,4,0,0"/></StackPanel>
     </Border>
     <ToggleButton x:Name="SidebarToggle" Height="44" ToolTip="Riduci o espandi la barra laterale">
      <StackPanel Orientation="Horizontal"><Path Data="{StaticResource IconChevron}" Style="{StaticResource SidebarItemIcon}"/><TextBlock Text="Riduci barra laterale" Style="{StaticResource SidebarLabel}"/></StackPanel>
     </ToggleButton>
    </StackPanel>
   </Grid>
  </Border>

  <Grid Grid.Column="1">
   <Grid.RowDefinitions><RowDefinition Height="82"/><RowDefinition Height="*"/></Grid.RowDefinitions>
   <Border Background="#091525" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
    <Grid Margin="24,0">
     <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
     <StackPanel VerticalAlignment="Center"><TextBlock Text="{Binding SelectedPage}" Style="{StaticResource PageTitleStyle}" Foreground="{StaticResource TextBrush}"/><TextBlock Text="FFGuardian Ultimate Protection" Style="{StaticResource CaptionTextStyle}" Foreground="{StaticResource MutedBrush}"/></StackPanel>
     <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
      <Button Style="{StaticResource IconButton}" Command="{Binding BackCommand}" ToolTip="Indietro"><TextBlock Text="‹" FontSize="24" Foreground="{StaticResource TextBrush}"/></Button>
      <Border Style="{StaticResource StatusChipStyle}" Margin="8,0"><TextBlock Text="{Binding ProtectionText}" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold"/></Border>
      <Button Style="{StaticResource IconButton}" Command="{Binding NavigateCommand}" CommandParameter="support" ToolTip="Aiuto"><Path Data="{StaticResource IconHelp}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.7" Width="19" Height="19"/></Button>
      <Button Style="{StaticResource IconButton}" Command="{Binding RefreshCommand}" ToolTip="Aggiorna stato"><Path Data="{StaticResource IconUpdate}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.7" Width="19" Height="19"/></Button>
     </StackPanel>
    </Grid>
   </Border>

   <Grid Grid.Row="1" Background="{StaticResource BackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
     <StackPanel Margin="16">
      <StackPanel.Style><Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding IsDashboard}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></StackPanel.Style>
      <WrapPanel>
       <Border Style="{StaticResource CardStyle}" Width="280" Height="170"><StackPanel><TextBlock Text="Protezione" Style="{StaticResource SectionTitleStyle}" Foreground="{StaticResource TextBrush}"/><TextBlock Text="{Binding ProtectionText}" Foreground="{StaticResource AccentBrush}" FontSize="20" FontWeight="SemiBold" Margin="0,12,0,6"/><TextBlock Text="{Binding ProtectionDetail}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap"/></StackPanel></Border>
       <Border Style="{StaticResource CardStyle}" Width="280" Height="170"><StackPanel><TextBlock Text="Motori di rilevamento" Style="{StaticResource SectionTitleStyle}" Foreground="{StaticResource TextBrush}"/><TextBlock Text="{Binding EngineVersion}" Foreground="{StaticResource TextBrush}" FontSize="20" FontWeight="SemiBold" Margin="0,12,0,6"/><TextBlock Text="Database: {Binding DatabaseVersion}" Foreground="{StaticResource MutedBrush}"/></StackPanel></Border>
       <Border Style="{StaticResource CardStyle}" Width="280" Height="170"><StackPanel><TextBlock Text="Scansione rapida" Style="{StaticResource SectionTitleStyle}" Foreground="{StaticResource TextBrush}"/><TextBlock Text="Ultima scansione: {Binding LastScan}" Foreground="{StaticResource MutedBrush}" Margin="0,12,0,12"/><Button Style="{StaticResource PrimaryButton}" Content="Avvia scansione" Command="{Binding QuickScanCommand}"/></StackPanel></Border>
      </WrapPanel>
      <Grid Margin="8"><Grid.ColumnDefinitions><ColumnDefinition Width="1.1*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Border Style="{StaticResource GlassCardStyle}"><StackPanel><TextBlock Text="Livello di sicurezza" Style="{StaticResource SectionTitleStyle}" Foreground="{StaticResource TextBrush}"/><TextBlock Text="{Binding SecurityScore}" FontSize="48" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/><TextBlock Text="{Binding ProtectionDetail}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap"/></StackPanel></Border><Border Grid.Column="1" Style="{StaticResource CardStyle}"><StackPanel><TextBlock Text="Attività recenti" Style="{StaticResource SectionTitleStyle}" Foreground="{StaticResource TextBrush}"/><ItemsControl ItemsSource="{Binding Activities}" Margin="0,12,0,0"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding}" Foreground="{StaticResource MutedBrush}" Margin="0,3"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel></Border></Grid>
     </StackPanel>
    </ScrollViewer>

    <local:ToolsView>
     <local:ToolsView.Style><Style TargetType="local:ToolsView"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding SelectedRoute}" Value="tools"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></local:ToolsView.Style>
    </local:ToolsView>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
     <ScrollViewer.Style>
      <Style TargetType="ScrollViewer"><Setter Property="Visibility" Value="Visible"/><Style.Triggers><DataTrigger Binding="{Binding IsDashboard}" Value="True"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger><DataTrigger Binding="{Binding SelectedRoute}" Value="tools"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style>
     </ScrollViewer.Style>
     <Grid Margin="24">
      <StackPanel MaxWidth="1100">
       <Border Style="{StaticResource GlassCardStyle}"><StackPanel><TextBlock Text="{Binding CurrentPage.Title}" Style="{StaticResource PageTitleStyle}" Foreground="{StaticResource TextBrush}"/><TextBlock Text="{Binding CurrentPage.Description}" Foreground="{StaticResource MutedBrush}" Margin="0,12,0,20" TextWrapping="Wrap"/><Border Style="{StaticResource StatusChipStyle}" HorizontalAlignment="Left"><StackPanel><TextBlock Text="Stato reale" Foreground="{StaticResource MutedBrush}" FontSize="11"/><TextBlock Text="{Binding ProtectionText}" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold"/><TextBlock Text="{Binding ProtectionDetail}" Foreground="{StaticResource MutedBrush}" MaxWidth="760" TextWrapping="Wrap"/></StackPanel></Border><StackPanel Orientation="Horizontal" Margin="0,22,0,0"><Button Style="{StaticResource PrimaryButton}" Content="{Binding CurrentPage.PrimaryAction}" Command="{Binding ActionCommand}" CommandParameter="{Binding CurrentPage.PrimaryAction}"/><Button Style="{StaticResource SecondaryButton}" Content="Aggiorna stato" Command="{Binding RefreshCommand}" Margin="12,0,0,0"/></StackPanel><TextBlock Text="{Binding LastAction}" Foreground="{StaticResource MutedBrush}" Margin="0,14,0,0" TextWrapping="Wrap"/></StackPanel></Border>
       <Border Style="{StaticResource CardStyle}" Margin="8"><StackPanel><TextBlock Text="Componenti disponibili" Style="{StaticResource SectionTitleStyle}" Foreground="{StaticResource TextBrush}"/><ItemsControl ItemsSource="{Binding Components}" Margin="0,12,0,0"><ItemsControl.ItemTemplate><DataTemplate><Grid Margin="0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="2*"/></Grid.ColumnDefinitions><TextBlock Text="{Binding Name}" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold"/><TextBlock Grid.Column="1" Text="{Binding IsOperational}" Foreground="{StaticResource MutedBrush}"/><TextBlock Grid.Column="2" Text="{Binding Detail}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap"/></Grid></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel></Border>
      </StackPanel>
     </Grid>
    </ScrollViewer>
   </Grid>
  </Grid>
 </Grid>
</Window>
