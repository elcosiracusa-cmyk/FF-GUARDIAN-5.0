name: Build FF GUARDIAN 6.2.1 Dashboard Layout Fix

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Publish single EXE
        run: dotnet publish FFGuardian.App/FFGuardian.App.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o publish
      - name: Install Inno Setup
        shell: powershell
        run: choco install innosetup -y --no-progress
      - name: Build installer
        shell: powershell
        run: '& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer\FFGuardianSetup.iss'
      - name: Create portable ZIP
        shell: powershell
        run: Compress-Archive -Path publish/* -DestinationPath FF-GUARDIAN-6.2.1-DASHBOARD-LAYOUT-FIX-PORTABLE.zip -Force
      - uses: actions/upload-artifact@v4
        with:
          name: FF-GUARDIAN-6.2.1-DASHBOARD-LAYOUT-FIX-INSTALLER
          path: installer/Output/FFGuardianSetup-6.2.1-Dashboard-Layout-Fix.exe
          if-no-files-found: error
          retention-days: 30
      - uses: actions/upload-artifact@v4
        with:
          name: FF-GUARDIAN-6.2.1-DASHBOARD-LAYOUT-FIX-PORTABLE
          path: FF-GUARDIAN-6.2.1-DASHBOARD-LAYOUT-FIX-PORTABLE.zip
          if-no-files-found: error
          retention-days: 30